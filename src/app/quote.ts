export class Quote {
  constructor(id: string, book: string, content: string) {
    this.id = id;
    this.book = book;
    this.content = content;
  }
  'id': string;
  'book': string;
  'content': string;
}
